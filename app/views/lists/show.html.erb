<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Url:</strong>
  <%= @list.url %>
</p>

<p>
  <strong>Content:</strong>
  <%= @list.content %>
</p>

<p>
  <strong>Description:</strong>
  <%= @list.description %>
</p>

<p>
  <strong>Stars:</strong>
  <%= @list.stars %>
</p>

<p>
  <strong>Parsed:</strong>
  <%= @list.parsed %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>

<ul>  
  <% @list.list_entry.each do |entry| %>
    <li><a href="<%= entry.url %>" target="_blank" > <%= entry.title %> </a>  <%= entry.description %>  </li>
  <% end %>
<ul>