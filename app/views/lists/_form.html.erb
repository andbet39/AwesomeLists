<%= form_with(model: list, local: true) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :list_name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :list_url %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :list_content %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :list_description %>
  </div>

  <div class="field">
    <%= form.label :stars %>
    <%= form.number_field :stars, id: :list_stars %>
  </div>

  <div class="field">
    <%= form.label :parsed %>
    <%= form.check_box :parsed, id: :list_parsed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
